//malloc = memory allocation                                                                              
//Asks the system for a chunk of memory and returns a pointer to it.                                      
char *text = malloc(100);  // give me 100 bytes 
free(text);